# ultrasound-telemetry-pandas-cleaner
An end-to-end data pipeline that streams live ultrasonic distance telemetry via a Serial interface, uses Python and Pandas to clean and deduplicate the raw hardware logs on-the-fly, and serves the data to a responsive Streamlit dashboard
